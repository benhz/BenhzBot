# Dify 集成指南（改进版）

## 🎯 核心改进

### 问题背景
在原始设计中，Dify 需要把 `user_id` 传回后台进行权限检查，这不够优雅：
```
钉钉消息 → 后台(已知user_id) → Dify → Dify把user_id传回后台 → 后台查权限
```

### 新架构方案
**基于会话上下文的权限检查**：后台维护会话映射，Dify 只需要告诉后台"做什么"，不需要传递"谁"：
```
钉钉消息 → 后台(注册session) → Dify → Dify调用后台(传conversation_id + action) → 后台自己查权限
```

---

## 🏗️ 架构设计

### 消息流程

```
┌────────────┐
│ 钉钉用户    │  @机器人 每周五15点半前完成周报
└─────┬──────┘
      │
      ↓
┌──────────────────────────────────────┐
│  后台 Message Handler                 │
│  1. 提取 user_id, conversation_id    │
│  2. 注册会话映射:                     │
│     conversation_id → user_id        │
│  3. 转发消息给 Dify（只传消息内容）   │
└──────────────┬───────────────────────┘
               │
               ↓
┌──────────────────────────────────────┐
│  Dify 大模型                          │
│  1. 理解意图: create_task            │
│  2. 提取参数:                         │
│     - name: "完成周报"                │
│     - cron_expr: "0 15 * * 5"        │
└──────────────┬───────────────────────┘
               │ 调用后台 API
               │ POST /api/v1/dify/execute
               │ {
               │   "conversation_id": "xxx",
               │   "action": "create_task",
               │   "params": {
               │     "name": "完成周报",
               │     "cron_expr": "0 15 * * 5"
               │   }
               │ }
               ↓
┌──────────────────────────────────────┐
│  后台 Dify Handler                    │
│  1. 根据 conversation_id 查找 user_id│
│  2. 查询权限: user_id + action       │
│  3. 权限检查:                         │
│     - 通过 → 执行操作                 │
│     - 不通过 → 返回拒绝消息          │
│  4. 返回结果给 Dify                   │
└──────────────┬───────────────────────┘
               │
               ↓
        Dify 回复用户
```

---

## 📡 API 接口

### 统一执行端点

**请求**:
```http
POST /api/v1/dify/execute
Content-Type: application/json
```

**请求体**:
```json
{
  "conversation_id": "钉钉会话ID",
  "action": "权限名称",
  "params": {
    "参数名": "参数值"
  }
}
```

**响应**:
```json
{
  "success": true,
  "message": "操作结果消息",
  "data": {},
  "reason": "失败原因（可选）"
}
```

---

## 🔑 支持的操作类型

### 1. 创建任务 (create_task)

**请求示例**:
```json
{
  "conversation_id": "cid1234567890",
  "action": "create_task",
  "params": {
    "name": "完成周报",
    "cron_expr": "0 15 * * 5",
    "type": "TASK"
  }
}
```

**响应示例**:
```json
{
  "success": true,
  "message": "✅ 任务创建成功！\n\n📋 名称: 完成周报\n⏰ Cron: 0 15 * * 5",
  "data": {
    "id": 1,
    "name": "完成周报",
    "cron_expr": "0 15 * * 5"
  }
}
```

### 2. 删除任务 (delete_task)

**请求示例**:
```json
{
  "conversation_id": "cid1234567890",
  "action": "delete_task",
  "params": {
    "task_id": 1
  }
}
```

### 3. 列出任务 (list_tasks)

**请求示例**:
```json
{
  "conversation_id": "cid1234567890",
  "action": "list_tasks",
  "params": {}
}
```

### 4. 打卡完成 (complete_task)

**请求示例**:
```json
{
  "conversation_id": "cid1234567890",
  "action": "complete_task",
  "params": {
    "task_id": 1
  }
}
```

### 5. 查看统计 (view_stats)

**请求示例**:
```json
{
  "conversation_id": "cid1234567890",
  "action": "view_stats",
  "params": {
    "task_id": 1
  }
}
```

### 6. 添加管理员 (add_admin)

**请求示例**:
```json
{
  "conversation_id": "cid1234567890",
  "action": "add_admin",
  "params": {
    "target_user_id": "user123",
    "target_username": "张三"
  }
}
```

### 7. 移除管理员 (remove_admin)

**请求示例**:
```json
{
  "conversation_id": "cid1234567890",
  "action": "remove_admin",
  "params": {
    "target_user_id": "user123"
  }
}
```

---

## 🚀 Dify 配置步骤

### 步骤 1: 创建 HTTP 工具

在 Dify 中添加 HTTP 工具：

**工具名称**: `execute_bot_action`

**URL**: `POST http://your-server:8080/api/v1/dify/execute`

**Headers**:
```
Content-Type: application/json
```

**Body Template**:
```json
{
  "conversation_id": "{{conversation_id}}",
  "action": "{{action}}",
  "params": {{params}}
}
```

### 步骤 2: 配置提示词

```
你是一个钉钉群助手机器人，帮助用户管理任务和打卡。

## 工作流程

当用户发送消息时：

1. **理解用户意图**
   - 提取用户想要执行的操作（action）
   - 提取相关参数（params）

2. **调用后台API**
   - 使用 execute_bot_action 工具
   - 传递 conversation_id（从系统变量获取）
   - 传递 action 和 params

3. **处理结果**
   - 如果 success=true，将 message 友好地展示给用户
   - 如果 success=false，告诉用户失败原因（reason）

## 支持的操作

| 用户说法 | action | params |
|---------|--------|--------|
| "创建任务..." | create_task | name, cron_expr, type |
| "删除任务..." | delete_task | task_id |
| "查看任务列表" | list_tasks | {} |
| "我已完成", "打卡" | complete_task | task_id |
| "查看统计" | view_stats | task_id |
| "添加管理员 @xxx" | add_admin | target_user_id, target_username |
| "移除管理员 @xxx" | remove_admin | target_user_id |

## 示例对话

**用户**: @机器人 每周五下午3点提醒完成周报
**助手思路**:
1. 用户想创建任务
2. 任务名称: "完成周报"
3. Cron表达式: "0 15 * * 5"（周五下午3点）
4. 调用 execute_bot_action(conversation_id, "create_task", {"name": "完成周报", "cron_expr": "0 15 * * 5", "type": "NOTIFICATION"})

**用户**: @机器人 我已完成今天的任务
**助手思路**:
1. 用户想打卡
2. 先调用 list_tasks 获取任务列表
3. 再调用 complete_task 打卡第一个任务

## 注意事项

- 不要向用户暴露技术细节（conversation_id, action 等）
- 权限不足时，友好地告诉用户原因
- Cron表达式格式要正确
- 始终传递 conversation_id（从系统变量获取）
```

### 步骤 3: 配置系统变量

在 Dify 中配置以下系统变量：
- `conversation_id`: 从钉钉消息中提取会话ID

---

## 💡 核心优势

### 1. 数据流向更合理
```
❌ 旧方案: user_id 在后台 → Dify → 再传回后台
✅ 新方案: user_id 始终在后台，Dify 只传递操作意图
```

### 2. 安全性更高
- user_id 不会暴露给 Dify
- 所有权限验证在后台完成
- 会话自动过期（30分钟无活动）

### 3. Dify 职责更清晰
```
Dify 的工作: 理解用户意图 → 提取参数 → 调用后台
后台的工作: 验证身份 → 检查权限 → 执行操作
```

### 4. 容错机制
即使 Dify 判断错误或跳过检查，后台仍会验证权限：
```
Dify 调用 → 后台从 session 查 user_id → 查权限 → 执行或拒绝
```

---

## 🔍 会话管理

### 会话存储机制

后台自动维护 `conversation_id` 到 `user_id` 的映射：

```go
type SessionInfo struct {
    UserID         string
    Username       string
    GroupChatID    string
    ConversationID string
    LastActiveTime time.Time
}
```

### 会话生命周期

1. **创建**: 用户发送 @ 机器人的消息时自动创建
2. **更新**: 每次 Dify 调用 API 时更新 `LastActiveTime`
3. **过期**: 30分钟无活动自动清理

### 会话查询

Dify 不需要关心会话管理，只需要在每次调用时传递 `conversation_id`，后台会自动查找对应的用户。

---

## 🛠️ 调试和测试

### 测试 API

使用 curl 测试：

```bash
# 模拟 Dify 调用创建任务
curl -X POST "http://localhost:8080/api/v1/dify/execute" \
  -H "Content-Type: application/json" \
  -d '{
    "conversation_id": "test_conversation_123",
    "action": "create_task",
    "params": {
      "name": "测试任务",
      "cron_expr": "0 9 * * *",
      "type": "TASK"
    }
  }'
```

**注意**: 测试前需要先发送一条钉钉消息，让后台注册该 `conversation_id`。

### 查看会话状态

会话信息存储在内存中，可以通过日志查看：

```
会话已保存: cid1234567890 → user123 (张三)
```

---

## 📊 对比：旧版 vs 新版

| 维度 | 旧版 API | 新版 API (推荐) |
|------|---------|----------------|
| **端点** | `/api/v1/permissions/check` | `/api/v1/dify/execute` |
| **Dify 需要传递** | `user_id` + `action` | `conversation_id` + `action` |
| **权限查询** | Dify 查询后决定 | 后台自动查询 |
| **user_id 来源** | Dify 从消息提取 | 后台从 session 查询 |
| **安全性** | 中等（user_id 暴露） | 高（user_id 不离开后台） |
| **Dify 配置复杂度** | 高（需要处理 user_id） | 低（只需理解意图） |
| **容错性** | 需要 Dify 正确传递 user_id | 自动容错（后台验证） |

---

## 🎓 最佳实践

### 1. Dify 只负责理解意图

```python
# ✅ 推荐
user_message = "每周五下午3点提醒完成周报"
action = "create_task"
params = {
    "name": "完成周报",
    "cron_expr": "0 15 * * 5"
}
call_api(conversation_id, action, params)

# ❌ 不推荐
user_id = extract_user_id(message)  # Dify 不应该处理 user_id
action = "create_task"
call_api(user_id, action, params)
```

### 2. 后台负责权限和执行

```go
// 后台自动处理
session := getSession(conversation_id)  // 自动查找 user_id
if !hasPermission(session.UserID, action) {
    return "权限不足"
}
executeAction(session.UserID, action, params)
```

### 3. 错误处理

Dify 应该优雅地处理错误响应：

```
后台返回: {"success": false, "message": "权限不足", "reason": "用户角色为 member，无权限执行 create_task"}

Dify 回复用户: "抱歉，您当前没有创建任务的权限。请联系管理员为您开通权限。"
```

---

## 🔄 迁移指南

如果你正在使用旧版 API，建议迁移到新版：

### 迁移步骤

1. **更新 Dify 工具配置**
   - URL: `/api/v1/permissions/check` → `/api/v1/dify/execute`
   - 参数: 移除 `user_id`，添加 `conversation_id`

2. **更新提示词**
   - 移除提取 `user_id` 的逻辑
   - 添加 `conversation_id` 系统变量

3. **测试验证**
   - 发送测试消息
   - 验证权限检查是否正常
   - 验证操作执行是否正常

### 兼容性说明

旧版 API 仍然保留并可用，但推荐使用新版 API 以获得更好的体验。

---

## 🆘 常见问题

### Q1: conversation_id 从哪里获取？
A: 钉钉消息中自带 `conversation_id`，后台会自动注册。Dify 只需要从系统变量中获取。

### Q2: 会话过期后怎么办？
A: 用户重新发送一条 @ 机器人的消息即可重新创建会话。

### Q3: 如何测试权限检查？
A: 先用普通用户测试，应该被拒绝。然后用管理员测试，应该成功。

### Q4: Dify 可以跳过权限检查吗？
A: 不能。即使 Dify 不检查，后台也会强制验证权限。

### Q5: 多个群的 conversation_id 会冲突吗？
A: 不会。每个群的 `conversation_id` 是唯一的。

---

## 📚 相关文档

- [完整 API 文档](./API_DOCUMENTATION.md)
- [权限系统说明](./API_DOCUMENTATION.md#权限系统说明)
- [部署指南](./API_DOCUMENTATION.md#部署和使用)

---

## 💬 反馈和支持

如有问题或建议，请提交 Issue 或联系开发团队。
